# Overlay applied on top of app.toml when the `prod` profile is active
# (APP_PROFILE=prod). Only the differences live here.

[server]
host = "0.0.0.0"

[database]
host = "db.internal"
name = "app"
pool_size = 32

[log]
format = "json"
